<?php
    $explanations = [
        "La gestion des erreurs en PHP est un sujet important. En effet, une erreur peut survenir à tout moment dans un script PHP. Il est donc important de savoir comment les gérer. Il existe plusieurs types d'erreurs en PHP :",
        " - Les erreurs fatales : elles interrompent l'exécution du script et affichent un message d'erreur. Par exemple, si un fichier inclus n'existe pas, une erreur fatale sera renvoyée.",
        " - Les avertissements : ils n'interrompent pas l'exécution du script mais affichent un message d'avertissement. Par exemple, si une variable non définie est utilisée, un avertissement sera renvoyé.",
        " - Les notices : elles sont généralement des messages d'information qui n'interrompent pas l'exécution du script. Par exemple, si une variable non définie est utilisée avec l'opérateur d'erreur <code>@</code>, la notice ne sera pas renvoyée et donc pas affichée.",
        "Pour gérer les erreurs en PHP, il est possible d'utiliser les fonctions d'erreur intégrées de PHP, telles que error_reporting() et set_error_handler(). Ces fonctions permettent de définir le niveau de rapport d'erreur et de personnaliser le gestionnaire d'erreurs.",        
    ];